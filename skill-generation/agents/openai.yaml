interface:
  display_name: "skill-generation"
  short_description: "Turn a dialog workflow into a reusable skill"
  default_prompt: "Use $skill-generation to summarize this workflow into a local skill with install sync for Codex and Claude Code."
