interface:
  display_name: "Paper Reading"
  short_description: "Evidence-grounded paper reading artifacts"
  default_prompt: "Use $paper-reading to create an HTML or Markdown report on this paper's claims, method, evidence, and limitations."

policy:
  allow_implicit_invocation: true
