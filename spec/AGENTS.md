# LLM Paper reading skill

This folder contains the skill for maintaining spec under a git codebase repo.

## WorkItems

1. Maintain the skill under `skill/`.
2. provide a `./sync` script to sync skills under `skills/` to user provided git repo folder for claude-code and code.
